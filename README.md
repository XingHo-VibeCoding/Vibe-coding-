# Claw

个人项目的实践仓库，按 30 天计划逐步搭建。

## 当前进度

| 日期 | 内容 | 状态 |
| --- | --- | --- |
| Day 1 | 环境准备（Git 安装与全局配置） | 已完成 |
| Day 2 | 仓库初始化 + 第一次提交（本页占位） | 已完成 |
| Day 7 | MVP 功能开发 | 待开始 |

## 目录结构

```
Claw/
├─ index.html      # 首页占位页（Day 2）
├─ .gitignore      # 忽略规则，已包含 .env
├─ README.md       # 本说明文件
└─ .env            # 本地配置文件，仅存在于本机，不会上传
```

## 注意

`.env` 用于存放本地敏感配置（数据库连接等），已在 `.gitignore` 中排除。
文件只存在于本机，永远不会被提交或推送到 GitHub。
